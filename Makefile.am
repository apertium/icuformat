ACLOCAL_AMFLAGS=-I m4

export LOCALES_DIR=$(datadir)/$(PACKAGE_NAME)
export LOCALES_DATA=$(LOCALES_DIR)/locales.dat

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = i18n.pc

SUBDIRS = src locales
